export default function Encryption() {
    return (
        <div>
            <h1>Encryption</h1>

            <p>Fhenix uses encrypted inputs</p>

            <pre>
                {`const encrypted = encrypt(value);`}
            </pre>

            <div>
                <p>Step 1: Encrypt</p>
                <p>Step 2: Send to contract</p>
            </div>
        </div>
    );
}